---
name: infographic-cards
description: >-
  把一份 Markdown 文档做成一套"素净版"信息图卡片（每节一张 PNG）并生成配图汇总页。
  触发词：出信息图、做配图、把文档做成图、信息图卡片、生成图片、出一套图、
  可视化文档、渲染 html 成 png、素净版信息图。
  典型场景：任何"一份 md 内容 → 一页页风格统一的图卡 + 一页汇总"的需求。
  说明：本 skill 是日常工作中总结沉淀的通用出图流程，示例均为占位内容。
---

# 信息图卡片流水线（md → HTML → PNG）

把一份 md 文档，做成一套风格统一、可单张交付的信息图卡片。核心是**三段式**：

```
NN-名.md   （唯一可编辑的内容源，改这里）
  └─ 中间产物/NN-*.html   （每节一张卡，套用共享 CSS）
       └─ 图片/NN-*.png   （Chrome headless 渲染，交付件）
            └─ 图片/_配图汇总.html   （一页浏览全部 PNG）
```

## 铁律（先读，全都踩过坑）

1. **md 是唯一内容源**。文案永远先改 md，再让 HTML/PNG 跟上。HTML 可以重组、拆页和压缩表达，但不能新增 md 中没有的事实或结论；出图时发现新内容，先写回 md。
2. **先核内容，再做视觉压缩**。研究、政策、技术和专利材料先检查事实、逻辑、来源与不确定性，再编排卡片。明确区分已核验事实、来源作者报告、合理推断和待核验线索；不要为了版面简洁删除决定结论边界的证据。
3. **对外名称必须自解释**。标题、导航、标签和结论优先使用受众能直接理解的概念名，不要让内部编号、文件号、缩写或项目代号承担主语义。必须保留时，放在完整名称之后的括号、脚注或元数据中。
4. **叙述使用直接陈述句**。标题优先写“现状、机制、关系、局限、结论、边界”，不要用连续反问或“为什么／如何／是什么”串联整套图。设问只在确有演示目的时少量使用；避免反复用“不是……而是……”制造演讲腔。
5. **内容多就拆页或扩宽，不要强行删减**。一张卡只承担一个主要结论。复杂综合内容按“组成与角色 → 汇合机制或流程 → 结果、责任与边界”分成多张；长表格、比较矩阵和多节点流程可以扩大画布。
6. **视口宽必须等于卡片宽**。默认宽度为 900px；高密度表格、比较矩阵或多节点流程可使用 1120–1280px。用 `--sheet-width` 设置卡片宽，并给渲染脚本传相同的 `-Width`。两者不一致会导致裁切或多余留白。
7. **风格素净**（这是刻意约束，勿回退）：
   - **不要 emoji / 图标字符**当装饰（一律不用）。
   - **不要多色色块 / 彩色端口**。整套只有一个强调色：红 `--red:#c02a2f`。
   - **不要块左侧红竖条**。要强调就把**标题标红**或**块内关键词标红**（`<b>`）。
   - **正文居中**（PPT 式），**标题保持左上角**（`.hd` 不动）。
8. **图别做复杂**。结构/关系图只要把关系讲清楚就行，别堆装饰元素、别叠一堆标签块。做过头了就删到只剩骨架 + 红字标注关键点。
9. **红字标点，不用装饰块标点**。要点出来的关键信息，用红色文字标，不要专门做个彩色小方块。
10. **关键词有层级，专有名词可解释**。关键数字、判断、技术机制和限制条件用 `<b>` 或 `.key` 加粗；专有名词用 `.term` 标记，并提供悬停、聚焦或触屏点击可打开的简短准确定义。
11. **网页交互不能破坏图片交付**。术语卡片默认隐藏，打印和 PNG 渲染时隐藏；HTML 汇报页使用一个共享卡片，支持鼠标、键盘和触屏，并能通过空白处点击、滚动或 `Esc` 关闭。
12. **必须检查移动端**。页面 `body.scrollWidth` 不得超过 `window.innerWidth`；桌面端检查卡片不遮挡导航，移动端检查术语卡片、标题、定义、表格和按钮不互相覆盖。

## 内容编排

在写 HTML 前完成一份卡片清单，每张只写四项：受众可读标题、一个主要结论、支撑该结论的必要证据、结论边界。按以下规则编排：

- 源文档本身有逻辑漏洞或事实风险时，先修源文档；不要用视觉设计掩盖问题。
- 压缩只删除重复表达，不删除关键限定、反例、核验状态和专利或研究边界。
- 一个总结涉及三条以上路径时，至少分别呈现各路径的角色、路径间的连接机制和汇合后的结果；不要把多条路径压成一页并列标签。
- 内部编号不得单独出现在主标题。例如把“方案 A 的价值”改为“跨项目经验受控复用方案的价值”，再在需要时标注“（方案 A）”。
- 导航、卡片标题、表头和结论使用同一套概念名称，避免同一对象在不同页面换名。
- 派生页末尾注明内容源和核查边界；展示稿不覆盖源文档的权威。

## 操作步骤

### 1. 建目录（每套图一个文件夹）

```
NN-名称/
  NN-名称.md        # 内容源
  中间产物/          # HTML + 共享 CSS
    _信息图样式.css
    01-xxx.html …
  图片/              # 渲染出的 PNG + 汇总页
    _配图汇总.html
  归档/
```

新建时把 `assets/_信息图样式.css` 复制进 `中间产物/`，把 `assets/渲染图片.ps1` 复制到该目录的上一级（多套图可共用一份）。需要 HTML 术语解释时，再把 `assets/term-cards.js` 复制到引用它的 HTML 所在目录。

### 2. 写 HTML（一节一张卡）

每张卡 = 一个 `.sheet`，`<head>` 里 `<link rel="stylesheet" href="_信息图样式.css">`，卡内特有布局写在页面内的 `<style>`。默认使用 900px；需要高密度画布时，在共享 CSS 之后覆盖变量：

```html
<style>:root{--sheet-width:1224px}</style>
```

骨架：

```html
<!DOCTYPE html><html lang="zh-CN"><head>
<meta charset="UTF-8"><title>…</title>
<link rel="stylesheet" href="_信息图样式.css">
</head><body>
<div class="sheet">
  <div class="hd">
    <div class="kk">SECTION KEY</div>
    <div class="tt"><h1>标题</h1><p class="sub">副标题</p></div>
  </div>
  <p class="lede">导语，正文居中，关键词用 <b>红字</b>。</p>
  <div class="cols">
    <div class="box"><div class="bh">小标题(红)</div><p>正文居中</p></div>
    …
  </div>
  <div class="note"><b>标注：</b>底部说明。</div>
</div>
</body></html>
```

#### 关键词与术语卡片

正文中只强调真正影响理解的词句，避免整段加粗。推荐写法：

```html
<p>系统保留 <b class="key">关键空间证据</b>，并把
  <span class="term" role="button" tabindex="0"
    data-title="空间经验单元"
    data-full="Spatial Experience Unit"
    data-def="带来源、适用条件、边界、版本和验证状态的可复用经验对象。">空间经验单元</span>
  交给当前任务重新核验。</p>

<!-- 每个 HTML 文档只放一个共享解释卡片；脚本放在 body 末尾。 -->
<div class="termcard" id="termcard" role="tooltip" aria-hidden="true">
  <div class="termcard-title"></div>
  <div class="termcard-full"></div>
  <div class="termcard-def"></div>
</div>
<script src="term-cards.js"></script>
```

复制 `assets/term-cards.js` 到 HTML 所在目录，并使用共享 CSS 中的 `.term`、`.termcard` 样式。每个术语必须有非空的 `data-title` 和 `data-def`；英文全称不确定时省略 `data-full`，不要猜写。PNG 没有悬停状态，重要定义仍应在正文、脚注或相邻卡片中保留。

现成组件（都在 CSS 里，直接用）：`.hd` 卡头 · `.lede` 居中导语 · `.axis`+`.chip` 标签行 · `.cols`+`.box`(`.bh` 红标题) 多栏 · `.bands`+`.band` 分层条 · `.chain` 箭头流程链 · `.tablewrap` 可横向滚动表格 · `.note` 底部标注。参考 `assets/示例-基础卡.html`（卡头 + 导语 + 多栏 + 标注）、`assets/示例-流程图.html`（主线 + 分支）和 `assets/示例-宽表格.html`（1224px 高密度画布 + 比较矩阵）。

可以将多张 `.sheet` 放进一个带导航的汇报 HTML 先审阅，也可以每张独立 HTML 后直接渲染 PNG。两种模式都必须服从同一 md 内容源。审阅型 HTML 需检查桌面和移动端：多栏在窄屏改为单栏，长表格在容器内横向滚动，页面本身不得产生不可控横向溢出。

### 3. 渲染 PNG

```powershell
Set-Location "…\NN-名称"
& "..\渲染图片.ps1" -Inputs "中间产物\*.html"     # 默认 900px
& "..\渲染图片.ps1" -Inputs "中间产物\*.html" -Width 1224  # 扩宽画布
# 单张：& "..\渲染图片.ps1" -Inputs "中间产物\03-流程.html"
# 内容密集时同步放大 CSS .sheet 与渲染视口
# & "..\渲染图片.ps1" -Inputs "中间产物\*.html" -Width 1200
```

脚本细节：Chrome `--headless=new`、`--force-device-scale-factor=2`、透明背景，从底部往上找非透明像素自动裁高。输出像素宽 = `Width × Scale`。`-Inputs` 是必填参数名，别省。找不到 Chrome 回退 Edge。

### 4. 生成 / 更新配图汇总页

`图片/_配图汇总.html`：一页按序展示全部 PNG，每张带编号 + 标题 + 对应 md 章节。参考 `assets/_配图汇总示例.html`。**关键**：图片 `width:calc(100% - 48px);margin:20px 24px 24px` —— 用 `100%` 会把图挤到卡片边缘无留白（踩过）。汇总页是独立 HTML、不套共享 CSS，改图数量时增删 `.fig` 块即可。

## 交付前检查

1. 对照 md 逐张确认：无新增事实、无结论漂移、无关键限定被删。
2. 扫描主标题、导航和标签：内部编号没有替代受众可读名称。
3. 扫描叙述语气：没有连续反问，标题直接说明内容。
4. 检查高密度页：优先拆页；确需扩宽时，CSS `--sheet-width` 与渲染 `-Width` 完全一致。
5. 检查关键词加粗有节制，专有名词均有定义；重要定义不只存在于悬停卡片中。
6. 在目标桌面宽度检查所有卡片无裁切、重叠和正文溢出；再以 390px 左右窄屏确认页面无整体横向溢出，术语卡片完全位于视口内且不遮挡导航。
7. 检查表格可滚动、导航锚点有效；术语卡片的悬停、键盘、触屏、空白点击、滚动和 `Esc` 开关正确，浏览器控制台无错误。
8. 分别检查 HTML 交互页与 PNG 静态页；更新全部 PNG 和汇总页，确认宽度、底部裁切和汇总页留白，避免旧图与新源文档混用。
